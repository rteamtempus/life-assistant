// TEMPLATE — copy to `environment.development.ts` and fill in. The real file
// is gitignored. Both values are public/client-safe (RLS gates everything).
export const environment = {
  production: false,
  supabaseUrl: 'https://<your-project-ref>.supabase.co',
  supabaseAnonKey: '<anon-public-key>',
};
